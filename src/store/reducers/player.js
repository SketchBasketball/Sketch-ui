// import { } from "../actions/player ";
const initialState = {
  isLoading: true,
  isError: false,
};
export default function playerReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
